name: "long-task"
description: "EXAMPLE: Run a long-running task in the background (for demonstration only)"
script_path: "examples/example_long_task.py"
endpoint: "task"
accepts_input: true
input_method: "json"
async: true
input_schema:
  task_name: "string"
  duration: "number"
