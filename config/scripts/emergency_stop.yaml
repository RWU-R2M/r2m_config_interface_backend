name: emergency_stop_script
description: Executes the emergency stop procedure.
script_path: emergency_stop.py  # Path relative to the scripts directory
endpoint: emergency_stop_script # API endpoint to trigger this script (removed the /api/scripts/ prefix)
accepts_input: false # Whether the script accepts input parameters 
input_method: json # How inputs are passed (json or env)
input_schema: {} # Define expected parameters if any
async: false # Whether the script runs asynchronously
expected_output: # Define the expected output structure for this synchronous script
  message: "string"
  status: "string"
